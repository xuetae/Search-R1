#!/usr/bin/env bash
set -euo pipefail

export IMAGE_PLATFORM="${IMAGE_PLATFORM:-linux/amd64}"
export SEARCHR1_IMAGE="${SEARCHR1_IMAGE:-searchr1-7b:cuda121}"
export RETRIEVER_IMAGE="${RETRIEVER_IMAGE:-searchr1-retriever:cuda121}"
export IMAGE_BUNDLE_DIR="${IMAGE_BUNDLE_DIR:-offline_upload/images}"
export DATA_BUNDLE_DIR="${DATA_BUNDLE_DIR:-offline_upload/data}"
export UPLOAD_BUNDLE_DIR="${UPLOAD_BUNDLE_DIR:-offline_upload}"

